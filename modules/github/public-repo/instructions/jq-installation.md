# JSON Query (jq) Installation Instructions

## Pre-requisites:

## Installation Steps:

Validate if jq is installed:
```bash
jq --version
```
If jq is not installed, install it using the following command:
```bash
sudo apt update && sudo apt upgrade -y
sudo apt install jq
```
Validate the installation:
```bash
jq --version
``` 