# Angular Module

## ToC
1. [Introduction](#1-introduction)
2. [Scope](#2-scope)
3. [Repo Map](#3-repo-map)
4. [Pre-requisites](#4-pre-requisites)
5. [Getting Started](#5-getting-started)

## 1. Introduction
This module provides the necessary tools, scripts, files and templates to work with Angular projects.

The Angular project is:
- Standalone components based project (no NgModules)
- SCSS based styling
- Runtime configuration (config.json) based project
- No SSR (Server Side Rendering) and no Zoneless project
- No zone.js (no need to install zone.js package)

## 2. Scope
Compare to default Angular CLI project, this module provides additional files and configurations to enhance the development experience and maintain code quality such as:

- Angular FS (extra to default Angular CLI project)
- VS Code - settings.json, extensions.json
- Prettier - .prettierrc, .prettierignore
- ESlint - .eslintrc.json, .eslintignore
- EditorConfig - .editorconfig
- TS config - tsconfig.app.json, tsconfig.spec.json
- API Contract (OpenAPI Swagger) - ./contracts/README.md
- Testing - package.json scripts for testing
- Production configuration - package.json scripts for production build
- NPM audit & dependabot - dependabot.yml, ci-npm-audit.yml

## 3. Repo Map
Below is the folder structure of Angular project with extra files and configurations provided by this module. It reflects what is generated by `ng new <project-name> --style=scss --ssr=false --zoneless=false` command and what is added or modified by this module.

```plaintext
.
├── contracts                             // extra to default
│   └── README.md                         // extra to default
├── docs                                  // extra to default
│   ├── api.md                            // extra to default
│   ├── architecture.md                   // extra to default
│   ├── development.md                    // extra to default 
│   ├── README.md                         // extra to default
│   ├── routing.md                        // extra to default
│   ├── styling.md                        // extra to default 
│   └── testing.md                        // extra to default
├── .editorconfig                         // extra to default
├── .gitignore
├── package.json
├── package-lock.json
├── .prettierignore                       // extra to default
├── .prettierrc                           // extra to default
├── public
│   ├── config                            // extra to default
│   │   └── config.json                   // extra to default
│   ├── data                              // extra to default
│   │   └── .gitkeep                      // extra to default
│   ├── favicon.ico
│   ├── icons                             // extra to default 
│   │   └── .gitkeep                      // extra to default
│   └── images                            // extra to default
│       └── .gitkeep                      // extra to default          
├── README.md                             // modified 
├── src
│   ├── app
│   │   ├── app.config.ts
│   │   ├── app.html
│   │   ├── app.routes.ts
│   │   ├── app.scss
│   │   ├── app.spec.ts
│   │   ├── app.ts
│   │   ├── core                          // extra to default
│   │   │   ├── config
│   │   │   │   ├── app-config.model.ts   // extra to default
│   │   │   │   └── app-config.service.ts // extra to default
│   │   │   ├── guards                    // extra to default
│   │   │   │   └── .gitkeep              // extra to default
│   │   │   ├── http                      // extra to default
│   │   │   │   └── .gitkeep              // extra to default
│   │   │   ├── interceptors              // extra to default
│   │   │   │   └── .gitkeep              // extra to default
│   │   │   ├── models                    // extra to default
│   │   │   │   ├── api-response.model.ts    // extra to default
│   │   │   │   └── error-response.model.ts  // extra to default
│   │   │   ├── services                     // extra to default
│   │   │   │   └── .gitkeep              // extra to default 
│   │   │   └── storage                   // extra to default
│   │   │       └── .gitkeep              // extra to default
│   │   ├── features                      // extra to default
│   │   │   ├── auth                      // extra to default
│   │   │   │   ├── auth.routes.ts        // extra to default
│   │   │   │   ├── components            // extra to default 
│   │   │   │   ├── models                // extra to default  
│   │   │   │   ├── pages                 // extra to default
│   │   │   │   └── services              // extra to default
│   │   │   ├── dashboard                 // extra to default
│   │   │   │   ├── components            // extra to default
│   │   │   │   ├── dashboard.routes.ts   // extra to default
│   │   │   │   ├── models                // extra to default
│   │   │   │   ├── pages                 // extra to default
│   │   │   │   └── services              // extra to default
│   │   │   └── users                     // extra to default
│   │   │       ├── components            // extra to default
│   │   │       ├── models                // extra to default
│   │   │       ├── pages                 // extra to default
│   │   │       ├── services              // extra to default
│   │   │       └── users.routes.ts       // extra to default
│   │   ├── shared                        // extra to default
│   │   │   ├── components                // extra to default
│   │   │   │   └── .gitkeep              // extra to default
│   │   │   ├── directives                // extra to default
│   │   │   │   └── .gitkeep              // extra to default
│   │   │   ├── pipes                     // extra to default
│   │   │   │   └── .gitkeep              // extra to default
│   │   │   ├── utils                     // extra to default    
│   │   │   │   └── .gitkeep              // extra to default
│   │   │   └── validators                // extra to default
│   │   │       └── .gitkeep              // extra to default
│   │   └── shell                         // extra to default
│   │       ├── auth-shell                // extra to default
│   │       │   └── components            // extra to default
│   │       └── main-shell                // extra to default
│   │           └── components            // extra to default
│   ├── index.html
│   ├── main.ts
│   ├── styles                            // extra to default
│   │   ├── _mixins.scss                  // extra to default
│   │   ├── _theme.scss                   // extra to default  
│   │   └── _variables.scss               // extra to default
│   └── styles.scss
├── tsconfig.app.json                     // modified
├── tsconfig.json
├── tsconfig.spec.json                    // modified
└── .vscode
    ├── extensions.json                   // modified 
    ├── launch.json                     
    ├── settings.json                     // modified
    └── tasks.json
```

## 4. Pre-requisites

1. [Repo bootstrap](../../README.md) done.
2. [Repo scaffold](../../instructions/scaffold/project-scaffold-guide.md) done.
3. Chromium based browser installed [Link](#33-install-chromium-based-browser-for-testing-optional)

## 5. Getting Started

### 1. Create Angular Default Project

Navigate to `apps/` directory and create a new Angular project using the Angular CLI command:
```bash
cd apps/
```
Create a new Angular project using the following command:
```bash
ng new <project-name> --style=scss --ssr=false --zoneless=false --dry-run  // e.g. "front-end"
```
Commit and push the changes to the repository.
```bash
git add .
git commit -m "feat: create angular project <project-name>"
git push origin <branch-name>  // e.g. "main"
```

### 2. Scaffold Angular Project with Custom Folder Structure

- Use script `bin/scaffold/scaffold_project.sh`
- Use template `modules/angular/templates/ng-custom-folder-structure.txt`

```bash
bash ./bin/scaffold/scaffold_project.sh --template "$HOME/<full-path-to-playbook-repo>/project-playbook/modules/angular/templates/ng-custom-folder-structure.txt" --path "$HOME/<full-path-to-repo>/apps/<ng-project-name>" --dry-run
```
Use `--dry-run` to see what changes will be made without actually making any changes. Once you are satisfied with the output, remove the `--dry-run` flag to apply the changes.

### 3. Manual extra steps

#### 3.1 Update package.json file

```json
scripts: {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "ng lint",
    "lint:fix": "ng lint --fix",
    "test": "ng test",
    "test:watch": "ng test --watch",
    "test:ci": "ng test --watch=false",
    "test:coverage": "ng test --watch=false --code-coverage",
    "build": "ng build",
    "build:prod": "ng build --configuration production"
}
```

#### 3.2 Install Prettier and ESlint

```bash
npm install --save-dev prettier
ng add angular-eslint --skip-confirmation
```

---

#### 3.3 Install Chromium based browser for testing (Optional)

```bash
sudo apt install chromium-browser
```
Validate installation:
```bash
chromium-browser --version
```

---

#### 3.4 Validate NPM scripts
To validate the NPM scripts, run the following commands in the terminal:

```bash
cd apps/<ng-project-name> 

npm run format:check
npm run lint
npm run test:ci
npm run build:prod
```  



#### 4.4 Prettier VS Code Extension

Install the Prettier - Code formatter extension in VS Code. You can find it in the Extensions Marketplace or by searching for "Prettier - Code formatter" in the Extensions view.   


#### 6.1 ESlint installation

Let Angular install ESLint for you using the following command:
```bash
ng add angular-eslint --skip-confirmation
```

Verify
```bash
npm run lint
```

